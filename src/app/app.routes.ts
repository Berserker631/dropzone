import { Routes } from '@angular/router';
import { DropAreaComponent } from '../shared/components/drop-area/drop-area.component';

export const routes: Routes = [
  {path: '', component: DropAreaComponent }
];
